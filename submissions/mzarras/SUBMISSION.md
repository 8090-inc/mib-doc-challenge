# Submission — mzarras

- **Solution repository** (public, contains `Dockerfile`): https://github.com/mzarras/mib-solution
- **Predictions**: `predictions.jsonl` in this folder — all 5,000 validation cases, generated by the Docker image at the repository head under the exact scoring contract (`--network none`, 4 vCPU, 8 GiB, read-only rootfs), ~1.5–1.6 s/PDF.
- **Technical memo**: `MEMO.md` in this folder (extended audit trail in the solution repo's `AUDIT.md`).
- Submission form: completed on the date of this PR.
