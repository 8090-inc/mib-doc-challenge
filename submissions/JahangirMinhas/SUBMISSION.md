# Submission

Solution repository: https://github.com/JahangirMinhas/mib-document-pipeline

The repository includes a `Dockerfile`; the built image accepts
`<input_pdf_dir> <output_predictions_path>` and runs fully offline (no
network access, no LLM/VLM calls). See the repository's `README.md` for
build/run instructions and `memo.md` for the technical writeup.
