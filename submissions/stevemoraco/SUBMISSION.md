# Submission — stevemoraco

Solution repository (public, Dockerfile-based, offline runtime): https://github.com/stevemoraco/mib-doc-solution

The image accepts `<input_pdf_dir> <output_predictions_path>` and runs with `--network none`.
