# Submission

**Solution repository:** https://github.com/matiaosa/mib-doc-challenge-solution

The repository contains:
- `solution.py` — full pipeline (text extraction, OCR fallback, field extraction, adjudication engine)
- `Dockerfile` — offline Docker build (no network access at runtime)
- `requirements.txt` — Python dependencies
- `MEMO.md` — technical approach and failure modes
